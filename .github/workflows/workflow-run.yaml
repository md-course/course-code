name: Worflow Run
on:
    workflow_run:
        workflows: ["Repository Events"]
        types: [completed]
jobs:
    echo-string:
        runs-on: ubuntu-latest
        steps:
            - name: Echo String
              run: echo "Workflow run triggered after Repository Events workflow completed"
